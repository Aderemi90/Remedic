import { Metadata } from "next"

const metadata: Metadata = {
  title: 'Services',
};



const services = () => {
  

  return (
    <div>
      <h1>User List</h1>
      <div> Services Page</div>
    </div>
  );
};

export default services;
